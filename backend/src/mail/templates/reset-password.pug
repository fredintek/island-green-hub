doctype html
html
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Reset Your Password
  body(style="font-family: Arial, sans-serif; background-color: #f4f4f4; text-align: center; padding: 20px;")
    .container(style="background: white; padding: 20px; border-radius: 8px; box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); max-width: 500px; margin: auto;")
      h1(style="color: #333;") Hello, #{user.firstname || user.email}!
      p(style="color: #555;") We received a request to reset your password. Copy the token below to proceed.
      p(style="color: #777;") If you did not request a password reset, please ignore this email.
      p(style="color: #777;") The reset token will expire in 1 hour.
      hr(style="border: none; border-top: 1px solid #ddd; margin: 20px 0;")
      p(style="word-break: break-all; color: #007bff;") #{resetPasswordToken}
      p(style="color: #777; font-size: 14px;") Best Regards,  
      p(style="color: #777; font-size: 14px; font-weight: bold;") The Team
